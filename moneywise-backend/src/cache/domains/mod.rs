// Domain-specific cache implementations for MoneyWise backend
// This module contains cache implementations for different business domains
// Each domain has its own cache service with domain-specific logic

pub mod budget;
